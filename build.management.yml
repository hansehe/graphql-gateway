run:
    selections:
        service:
            files:
                - docker-compose.yml

build:
    selections:
        helm:
            directory: helm
            cmd: 
                - helm template graphql-gateway
                - helm package graphql-gateway --destination charts
                - helm repo index ./charts
